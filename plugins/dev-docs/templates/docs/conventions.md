# Conventions

<!-- Only rules that are real. A rule nobody follows teaches readers to
     distrust this whole file. If it's enforced by a tool, say which tool and
     let the tool be the authority.

     Delete any section that doesn't apply. -->

## Commands

<!-- The handful a newcomer needs on day one. Build, run, test, format.
     Literal commands, copy-pasteable. -->

```
build:
run:
test:
format:
```

## Naming

<!-- Only the non-obvious. Nobody needs to be told to use clear names. Worth
     recording: casing rules per kind of thing, required prefixes or suffixes,
     and any word you use in a specific technical sense. -->

## File layout

<!-- Where a new file goes and why. Rules, not a tree listing:
     "one class per file, filename matches the class"
     "tests live beside the code they test" -->

## Style

<!-- Formatter and linter, plus anything they can't enforce. If there's a
     config file, point at it rather than restating it. -->

## Git

<!-- DELETE IF YOU HAVE A SEPARATE GIT DOC AND LINK TO IT INSTEAD.

     Branch naming, commit message format, what needs review before merge. -->

## Terms

<!-- Words that mean something specific here. Especially words with an everyday
     meaning you've narrowed: "session", "entity", "job", "actor". Two or three
     lines each. -->
